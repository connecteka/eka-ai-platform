[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.build]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && gunicorn wsgi:application -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8001 --workers 2 --timeout 120"
