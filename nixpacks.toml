# CACHE BUST: 2026-02-16-T17:30:00 - NEW UI DEPLOYMENT WITH PRICING & SUBSCRIPTION
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["rm -rf dist node_modules/.cache && npm ci --legacy-peer-deps --no-cache"]

[phases.build]
cmds = ["rm -rf dist && NODE_ENV=production npm run build && echo '=== BUILD OUTPUT ===' && ls -la dist/"]

[start]
cmd = "npx serve -s dist -l $PORT --no-cache"
