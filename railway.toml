[build]
builder = "nixpacks"
buildCommand = "rm -rf dist node_modules/.cache && npm ci --legacy-peer-deps --no-cache && NODE_ENV=production npm run build"

[deploy]
startCommand = "npx serve -s dist -l $PORT"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
